#include <functional>
#include <string>
#include <utility>
#include "bytecode/ByteCode.h"

void createTask(std::string name, std::function<void(std::string)> task) {
  task(std::move(name));
}
using namespace torchlight::bytecode::WriteByteCodeOperatorAdd;
int main() {

      if (true) {
        // do nothing
      }
    });
  return 0;
}
